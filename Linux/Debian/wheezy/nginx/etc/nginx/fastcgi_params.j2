# {{ ansible_managed }}

fastcgi_param   QUERY_STRING            $query_string;
fastcgi_param   REQUEST_METHOD          $request_method;
fastcgi_param   CONTENT_TYPE            $content_type;
fastcgi_param   CONTENT_LENGTH          $content_length;

fastcgi_param   SCRIPT_FILENAME         $request_filename;
fastcgi_param   SCRIPT_NAME             $fastcgi_script_name;
fastcgi_param   REQUEST_URI             $request_uri;
fastcgi_param   DOCUMENT_URI            $document_uri;
fastcgi_param   DOCUMENT_ROOT           $document_root;
fastcgi_param   SERVER_PROTOCOL         $server_protocol;

fastcgi_param   GATEWAY_INTERFACE       CGI/1.1;
fastcgi_param   SERVER_SOFTWARE         nginx/$nginx_version;

fastcgi_param   REMOTE_ADDR             $remote_addr;
fastcgi_param   REMOTE_PORT             $remote_port;
fastcgi_param   SERVER_ADDR             $server_addr;
fastcgi_param   SERVER_PORT             $server_port;
fastcgi_param   SERVER_NAME             $server_name;

fastcgi_param   HTTPS                   $https if_not_empty;

# PHP only, required if PHP was built with --enable-force-cgi-redirect
fastcgi_param   REDIRECT_STATUS         200;

####################
## FOURNISSEUR Tuning ##
####################
fastcgi_connect_timeout {{ fastcgi_connect_timeout|default('600') }};
fastcgi_send_timeout {{ fastcgi_send_timeout|default('600') }};
fastcgi_read_timeout {{ fastcgi_read_timeout|default('600') }};
fastcgi_buffer_size {{ fastcgi_buffer_size|default('1024k') }};
fastcgi_buffers {{ fastcgi_buffers|default('64 512k') }};
fastcgi_busy_buffers_size {{ fastcgi_busy_buffers_size|default('1024k') }};
fastcgi_temp_file_write_size {{ fastcgi_temp_file_write_size|default('1024k') }};
fastcgi_intercept_errors {{ fastcgi_intercept_errors|default('on') }};
fastcgi_ignore_client_abort {{ fastcgi_ignore_client_abort|default('on') }};
